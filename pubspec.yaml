name: linker
description: XRay Proxy Config Editor — cross-platform Flutter app
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_riverpod:
  riverpod_annotation:
  go_router:
  dio:
  flutter_secure_storage:
  shared_preferences:
  re_editor:
  re_highlight:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  build_runner:
  riverpod_generator:

flutter:
  uses-material-design: true
